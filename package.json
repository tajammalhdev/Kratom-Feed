{
  "name": "kratom-feed-theme",
  "version": "1.0.0",
  "private": true,
  "description": "Kratom Feed WordPress theme â€” Tailwind CSS build",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./assets/src/css/input.css -o ./assets/css/tailwind.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./assets/src/css/input.css -o ./assets/css/tailwind.css --watch",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.7",
    "@tailwindcss/typography": "^0.5.20",
    "tailwindcss": "^4.3.2"
  }
}
